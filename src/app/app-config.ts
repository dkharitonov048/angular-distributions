interface AppConfig{
    baseUrl:string;
    token: string;
}

// fill this props
export const APPCONFIG: AppConfig = {
    baseUrl: "http://gatew-------------------------.ru",
    token: "Bearer ..."
};